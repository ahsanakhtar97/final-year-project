// Intentionally empty. Types for `three` come from @types/three (devDep).
// This file exists only so the build still resolves "three" if @types/three
// hasn't been installed yet (`npm install` will fix that).
